spring:
    application:
        name: credit-service

server:
    port: 8086
    error:
        include-message: always


custom:
    account-service: ${ACCOUNT_SERVICE:http://localhost:8081}
    order-service: ${ORDER_SERVICE:http://localhost:8081}